[workspace]
members = [
    "crates/winspaces-common",
    "crates/winspaces-daemon",
    "crates/winspaces-gui",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
